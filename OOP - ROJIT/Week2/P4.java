package Week2;

public class P4 {
    public static void main(String[] args) {
        // Variable representing age
        int age = 0;

        // Check if age is greater than or equal to 18 using ternary operator
        String result = (age >= 18) ? "You are an adult" : "You are not an adult";

        // Print the result
        System.out.println(result);
    }
}
